"""Domain layer: pure types and invariants.

This package imports nothing from ``api``, ``collection``, ``orchestration``, or
``analyzers``. The dependency arrow points one way, which is what keeps the models usable
in tests without booting anything.
"""
